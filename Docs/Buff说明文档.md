# Buff 说明文档

## Buff 基础设计

所有 Buff 都继承自基类 `BaseBuff`，包含以下基础属性：

| 属性 | 类型 | 说明 |
| --- | --- | --- |
| `buffId` | int | Buff 唯一标识符 |
| `buffName` | string | Buff 名称 |
| `buffDescription` | string | Buff 描述文本 |
| `buffIcon` | string | Buff 图标在Resources/BuffIcon目录下的路径，不包含后缀 |
| `buffDuration` | float | Buff 持续时间（-1表示永久，0表示一次性buff） |
| `buffStackable` | bool | 是否可叠加 |
| `stackType` | int | 计算方式（"0"表示加法叠加，"1"表示乘法叠加） |
| `isNormalBuff` | bool | 是否为普通buff（普通buff只在非boss关卡出现） |

## Buff详细配置

### 1. 强力射速（buffId:1）
- 说明：增加子弹基础和狂暴发射速度

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | float | 子弹发射速度倍率 |

### 2. 不要靠近（buffId:2）
- 说明：减少敌人移动速度

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | float | 减少的移动速度值 |

### 3. 强力暴击（buffId:3）
- 说明：子弹有概率暴击，无视颜色直接连爆

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | float | 暴击概率百分比 |

### 4. 移速增加（buffId:4）
- 说明：增加玩家人物的移动速度

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | float | 增加的移动速度值 |

### 5. 蹦蹦炸弹（buffId:5）
- 说明：连爆范围更大（爆一个变为爆两个）

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | int | 爆炸范围倍率 |

### 6. 分裂增速（buffId:6）
- 说明：子弹分裂后速度大幅增加

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | float | 分裂后速度倍率 |

### 7. 更加狂暴（buffId:7）
- 说明：增加狂暴模式时间

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | float | 狂暴时间增加值 |

### 8. 强劲体力（buffId:8）
- 说明：人物血量增加

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | int | 增加的血量值 |

### 9. 再次翻牌（buffId:9）
- 说明：重新刷新三个buff

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | int | 刷新次数 |

### 10. 更加清汤（buffId:10）
- 说明：在清汤场景（储备子弹模式）时间限制延长，可以分裂更多子弹

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | float | 时间延长倍率 |

### 11. 多多子弹（buffId:11）
- 说明：将当前子弹数乘指定值

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | int | 子弹数倍率 |

### 12. 快速回血（buffId:12）
- 说明：增加回血数

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | int | 增加的回血数值 |

### 13. 足够强大（buffId:13）
- 说明：无任何效果。选择此buff五次可获得一个奖杯

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | int | 选择次数计数 |

### 14. 我要变小（buffId:14）
- 说明：缩小人物体型，使其更难受到伤害

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | float | 体型缩小倍率 |

### 15. 远离敌人（buffId:15）
- 说明：boss获得击退效果

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | float | 击退力度 |

### 16. 足够倒霉（buffId:16）
- 说明：无任何效果，占用一个buff位置

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | int | 无实际作用 |

### 17. 碰碰运气（buffId:17）
- 说明：下一关有70%几率使子弹数乘5倍，但也有30%失去一半的子弹

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | float | 成功概率（0.7） |

### 18. 克制敌人（buffId:18）
- 说明：大幅降低boss的移动速度

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | float | 降低的移动速度值 |

### 19. 多多清汤（buffId:19）
- 说明：减少进入清汤锅的冷却时间

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | float | 冷却时间减少值 |

### 20. 伤害增加（buffId:20）
- 说明：增加子弹对boss伤害

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `value` | int | 增加的伤害值 |





